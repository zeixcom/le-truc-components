import { Meta, Canvas, Controls } from "@storybook/addon-docs/blocks";
import * as FormInplaceEditStories from "./form-inplace-edit.stories";

<Meta of={FormInplaceEditStories} />

### Form Inplace Edit

A self-contained inline label editor — click to edit, and the label seamlessly becomes a text field. It shows how a component can swap its own internal presentation in response to user interaction.

#### Tag Name

`form-inplace-edit`

#### Preview

<Canvas of={FormInplaceEditStories.Default} />

#### Controls

<Controls of={FormInplaceEditStories.Default} />

#### Reactive Properties

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Type</th>
      <th>Default</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>editing</code></td>
      <td><code>boolean</code></td>
      <td>Attribute <code>editing</code></td>
      <td>Whether edit mode is currently activated</td>
    </tr>
  </tbody>
</table>

#### Descendant Elements

<table>
  <thead>
    <tr>
      <th>Selector</th>
      <th>Type</th>
      <th>Required</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>first('.text')</code></td>
      <td><code>HTMLElement</code></td>
      <td><strong>required</strong></td>
      <td>Label display element; hidden during editing</td>
    </tr>
    <tr>
      <td><code>first('button')</code></td>
      <td><code>HTMLButtonElement</code></td>
      <td>optional</td>
      <td>Toggle button: ✎ (view mode) / ✓ (edit mode)</td>
    </tr>
  </tbody>
</table>
