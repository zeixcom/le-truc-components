import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as FormListboxStories from './form-listbox.stories';

<Meta of={FormListboxStories} />

### Form Listbox

A full-featured listbox with client-side filtering, optional remote option loading, and keyboard navigation. It demonstrates form participation and per-option reactive state within a single component.

#### Tag Name

`form-listbox`

#### Preview

<Canvas of={FormListboxStories.Default} />

#### Controls

<Controls of={FormListboxStories.Default} />

#### Reactive Properties

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Type</th>
      <th>Default</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>options</code></td>
      <td><code>HTMLButtonElement[]</code> (readonly)</td>
      <td>Visible <code>button[role="option"]</code> elements</td>
      <td>Live collection of selectable (non-hidden) options</td>
    </tr>
  </tbody>
</table>

#### Events

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Detail type</th>
      <th>When</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>change</code></td>
      <td><em>none</em> (plain <code>Event</code>)</td>
      <td>Dispatched from the host when an option is selected (click or Enter key); bubbles so composing components like <code>form-combobox</code> can listen without reaching into the listbox's DOM</td>
    </tr>
  </tbody>
</table>

#### Descendant Elements

<table>
  <thead>
    <tr>
      <th>Selector</th>
      <th>Type</th>
      <th>Required</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>first('[role="listbox"]')</code></td>
      <td><code>HTMLElement</code></td>
      <td><strong>required</strong></td>
      <td>Container for option buttons</td>
    </tr>
    <tr>
      <td><code>first('input.filter')</code></td>
      <td><code>HTMLInputElement</code></td>
      <td>optional</td>
      <td>Text input for client-side filtering</td>
    </tr>
    <tr>
      <td><code>first('button.clear')</code></td>
      <td><code>HTMLButtonElement</code></td>
      <td>optional</td>
      <td>Clears current filter; hidden when filter is empty</td>
    </tr>
    <tr>
      <td><code>first('card-callout')</code></td>
      <td><code>HTMLElement</code></td>
      <td>optional</td>
      <td>Loading/error callout when <code>src</code> is used; gets <code>danger</code> class on error</td>
    </tr>
    <tr>
      <td><code>first('.loading')</code></td>
      <td><code>HTMLElement</code></td>
      <td>optional</td>
      <td>Shown while <code>src</code> fetch is pending</td>
    </tr>
    <tr>
      <td><code>first('.error')</code></td>
      <td><code>HTMLElement</code></td>
      <td>optional</td>
      <td>Shown and populated with error text when <code>src</code> fetch fails</td>
    </tr>
    <tr>
      <td><code>all('button[role="option"]')</code></td>
      <td><code>Memo&lt;HTMLButtonElement[]&gt;</code></td>
      <td>optional</td>
      <td>Option buttons (inline and/or rendered from <code>src</code>)</td>
    </tr>
  </tbody>
</table>
