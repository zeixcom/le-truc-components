import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as ModulePaginationStories from './module-pagination.stories';

<Meta of={ModulePaginationStories} />

### Module Pagination

A keyboard-navigable pagination control with previous/next buttons and a clamped numeric input. It hides itself automatically when there's nothing to paginate.

#### Tag Name

`module-pagination`

#### Preview

<Canvas of={ModulePaginationStories.Default} />

#### Controls

<Controls of={ModulePaginationStories.Default} />

#### Descendant Elements

<table>
  <thead>
    <tr>
      <th>Selector</th>
      <th>Type</th>
      <th>Required</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>first('input')</code></td>
      <td><code>HTMLInputElement</code></td>
      <td><strong>required</strong></td>
      <td>Number input used to initialise and display <code>value</code> and <code>max</code></td>
    </tr>
    <tr>
      <td><code>first('button.prev')</code></td>
      <td><code>HTMLButtonElement</code></td>
      <td><strong>required</strong></td>
      <td>Decrements <code>value</code>; disabled when <code>value ≤ 1</code></td>
    </tr>
    <tr>
      <td><code>first('button.next')</code></td>
      <td><code>HTMLButtonElement</code></td>
      <td><strong>required</strong></td>
      <td>Increments <code>value</code>; disabled when <code>value ≥ max</code></td>
    </tr>
    <tr>
      <td><code>first('.value')</code></td>
      <td><code>HTMLElement</code></td>
      <td>optional</td>
      <td>Text node updated with the current page number</td>
    </tr>
    <tr>
      <td><code>first('.max')</code></td>
      <td><code>HTMLElement</code></td>
      <td>optional</td>
      <td>Text node updated with the total page count</td>
    </tr>
  </tbody>
</table>
