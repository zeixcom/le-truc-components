import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as BasicCounterStories from './basic-counter.stories';

<Meta of={BasicCounterStories} />

### Basic Counter

Le Truc's canonical introductory example: a button that increments a counter and a display that stays in sync. It demonstrates the core reactive loop — read the initial value from the DOM, update it on user interaction, and bind it back to the display.

#### Tag Name

`basic-counter`

#### Preview

<Canvas of={BasicCounterStories.Default} />

#### Controls

<Controls of={BasicCounterStories.Default} />

#### Descendant Elements

<table>
  <thead>
    <tr>
      <th>Selector</th>
      <th>Type</th>
      <th>Required</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>first('button')</code></td>
      <td><code>HTMLButtonElement</code></td>
      <td><strong>required</strong></td>
      <td>Increments the count on click</td>
    </tr>
    <tr>
      <td><code>first('span')</code></td>
      <td><code>HTMLSpanElement</code></td>
      <td><strong>required</strong></td>
      <td>Displays the current count; its initial text content seeds the <code>count</code> property</td>
    </tr>
  </tbody>
</table>
