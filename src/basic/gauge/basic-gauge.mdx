import { Meta, Canvas, Controls } from "@storybook/addon-docs/blocks";
import * as BasicGaugeStories from "./basic-gauge.stories";

<Meta of={BasicGaugeStories} />

### Basic Gauge

A visual gauge meter that renders a value against configurable, colored threshold ranges. It shows how a single attribute can drive both a displayed value and its derived visual styling.

#### Tag Name

`basic-gauge`

#### Preview

<Canvas of={BasicGaugeStories.Default} />

#### Controls

<Controls of={BasicGaugeStories.Default} />

`thresholds` is a JSON array of objects sorted from highest to lowest `min`. Each entry has `min` (number), `label` (string), and `color` (CSS color string). The first entry whose `min` is ≤ `host.value` determines the active label and color. Example: `[{"min":0.8,"label":"Good job!","color":"var(--color-green-70)"},{"min":0.5,"label":"Decent","color":"var(--color-orange-70)"},{"min":0,"label":"Try again!","color":"var(--color-pink-70)"}]`

See the `Decent` and `TryAgain` stories for variants with the same thresholds applied to different values.

#### Descendant Elements

<table>
  <thead>
    <tr>
      <th>Selector</th>
      <th>Type</th>
      <th>Required</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>first('meter')</code></td>
      <td><code>HTMLMeterElement</code></td>
      <td><strong>required</strong></td>
      <td>Native meter element; its <code>value</code> seeds <code>host.value</code> at initialisation and stays in sync inside <code>watch('value')</code></td>
    </tr>
    <tr>
      <td><code>first('basic-number')</code></td>
      <td><code>HTMLElementTagNameMap['basic-number']</code></td>
      <td><strong>required</strong></td>
      <td><code>&lt;basic-number&gt;</code> child component that displays the formatted percentage; receives <code>host.value / 100</code> via <code>pass()</code> — configure display via the <code>options</code> attribute on <code>&lt;basic-number&gt;</code></td>
    </tr>
    <tr>
      <td><code>first('.label')</code></td>
      <td><code>HTMLElement</code></td>
      <td><strong>required</strong></td>
      <td>Displays the active threshold label</td>
    </tr>
  </tbody>
</table>
