import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as BasicHelloStories from './basic-hello.stories';

<Meta of={BasicHelloStories} />

### Basic Hello

The Hello World example from the Quick Start guide — a greeting that updates live as the user types. It's the smallest possible Le Truc component, showing the minimal read-react-bind pattern.

#### Tag Name

`basic-hello`

#### Preview

<Canvas of={BasicHelloStories.Default} />

#### Controls

<Controls of={BasicHelloStories.Default} />

#### Descendant Elements

<table>
  <thead>
    <tr>
      <th>Selector</th>
      <th>Type</th>
      <th>Required</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>first('input')</code></td>
      <td><code>HTMLInputElement</code></td>
      <td><strong>required</strong></td>
      <td>Text field to enter the name; updates <code>host.subject</code> on each <code>input</code> event, falling back to the initial value when empty</td>
    </tr>
    <tr>
      <td><code>first('output')</code></td>
      <td><code>HTMLOutputElement</code></td>
      <td><strong>required</strong></td>
      <td>Displays the current name; its initial text content seeds the <code>subject</code> property</td>
    </tr>
  </tbody>
</table>
