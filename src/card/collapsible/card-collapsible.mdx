import { Meta, Canvas, Controls } from "@storybook/addon-docs/blocks";
import * as CardCollapsibleStories from "./card-collapsible.stories";

<Meta of={CardCollapsibleStories} />

### Card Collapsible

A content-agnostic collapsible card wrapping a native `<details>`/`<summary>` element.

#### Tag Name

`card-collapsible`

#### Preview

<Canvas of={CardCollapsibleStories.Default} />

#### Controls

<Controls of={CardCollapsibleStories.Default} />

See the `OpenByDefault` story for a variant that starts expanded.

#### Descendant Elements

<table>
  <thead>
    <tr>
      <th>Selector</th>
      <th>Type</th>
      <th>Required</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>first('details')</code></td>
      <td><code>HTMLDetailsElement</code></td>
      <td><strong>required</strong></td>
      <td>Native disclosure element; put the header in <code>&lt;summary&gt;</code>, the body as sibling content</td>
    </tr>
  </tbody>
</table>
