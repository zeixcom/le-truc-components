import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import * as CardColorscaleStories from "./card-colorscale.stories";

<Meta of={CardColorscaleStories} />

### Card Colorscale

A color scale card that displays a 9-step tonal palette derived from a single base OKLCH color.

#### Tag Name

`card-colorscale`

#### Preview

<Canvas of={CardColorscaleStories.Default} />

#### Reactive Properties

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Type</th>
      <th>Default</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>name</code></td>
      <td><code>string</code></td>
      <td>Text content of <code>.label strong</code></td>
      <td>Display name of the color</td>
    </tr>
    <tr>
      <td><code>color</code></td>
      <td><code>Oklch</code></td>
      <td><code>asOklch()</code> — parsed from <code>color</code> attribute</td>
      <td>Base color; drives all CSS custom properties and the hex label</td>
    </tr>
  </tbody>
</table>

#### Classes

<table>
  <thead>
    <tr>
      <th>Class</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>tiny</code></td>
      <td>Smallest swatch size</td>
    </tr>
    <tr>
      <td><code>small</code></td>
      <td>Small swatch size</td>
    </tr>
    <tr>
      <td><code>medium</code></td>
      <td>Medium swatch size</td>
    </tr>
    <tr>
      <td><code>large</code></td>
      <td>Largest swatch size</td>
    </tr>
  </tbody>
</table>

#### Descendant Elements

<table>
  <thead>
    <tr>
      <th>Selector</th>
      <th>Type</th>
      <th>Required</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>first('.label strong')</code></td>
      <td><code>HTMLElement</code></td>
      <td><strong>required</strong></td>
      <td>Displays the color name; bound to <code>name</code></td>
    </tr>
    <tr>
      <td><code>first('.label small')</code></td>
      <td><code>HTMLElement</code></td>
      <td><strong>required</strong></td>
      <td>Displays the hex color value; updated on <code>color</code> change</td>
    </tr>
    <tr>
      <td><code>li.lighten80</code> … <code>li.lighten20</code></td>
      <td><code>HTMLLIElement</code></td>
      <td>optional</td>
      <td>Lightened tonal steps; background set via <code>--color-lighten*</code></td>
    </tr>
    <tr>
      <td><code>li.base</code></td>
      <td><code>HTMLLIElement</code></td>
      <td>optional</td>
      <td>Base color step; background set via <code>--color-base</code></td>
    </tr>
    <tr>
      <td><code>li.darken20</code> … <code>li.darken80</code></td>
      <td><code>HTMLLIElement</code></td>
      <td>optional</td>
      <td>Darkened tonal steps; background set via <code>--color-darken*</code></td>
    </tr>
  </tbody>
</table>
